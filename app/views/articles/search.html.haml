= render partial: "header"

.mypage-main
  .my-body
    .my-articles
      - if @articles.present?
        %h3.my-articles__head
          キーワードにヒットした記事一覧
        - @articles.each do |article|
          .my-a-item
            %a{href: "/articles/#{article.id}", class: "my-a-item__title"} #{article.title}
            .my-a-info
              %p.i.fa.fa-thumbs-up 3
      - else
        %h3.my-articles__head
          一致する記事はありませんでした。

= render partial: "footer"
